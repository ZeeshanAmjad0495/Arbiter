# Minimal Prometheus — scrapes itself; add Zuub targets/service-discovery as needed.
global:
  scrape_interval: 30s
scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']
